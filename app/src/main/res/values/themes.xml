<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.fida" parent="Theme.Material3.DayNight">
        <!--        &lt;!&ndash; Customize your light theme here. &ndash;&gt;-->
        <!--         <item name="android:navigationBarColor">@color/white</item>-->
        <!--        <item name="actionBarStyle">@style/MyActionBar</item>-->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Default button styles with proper text color for light/dark themes -->
        <item name="materialButtonStyle">@style/Widget.fida.Button</item>
        <item name="borderlessButtonStyle">@style/Widget.fida.Button.Text</item>
    </style>

    <style name="Theme.fida" parent="Base.Theme.fida" />

    <!-- MaterialButton style with proper text color for light/dark themes -->
    <style name="Widget.fida.Button" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/blue</item>
    </style>

    <style name="Widget.fida.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/blue</item>
    </style>

    <style name="Widget.fida.Button.Text" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/blue</item>
    </style>

    <!-- TextInputLayout style with darker border for health questionnaire -->
    <style name="Widget.fida.TextInputLayout" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="boxStrokeColor">@color/blue</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="android:textColor">#222222</item>
        <item name="android:textColorHint">#888888</item>
    </style>
</resources>